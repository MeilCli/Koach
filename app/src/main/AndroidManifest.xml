<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="jp.dely.koach.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AnchorActivity" />
        <activity android:name=".RelativeActivity" />
        <activity android:name=".AnimationActivity" />
        <activity android:name=".ShapeActivity" />
        <activity android:name=".AnimationShapeActivity" />
        <activity android:name=".AnimationSceneActivity" />
        <activity android:name=".ClickTargetActivity" />
        <activity android:name=".ViewGroupActivity" />
        <activity android:name=".CoachViewActivity" />
    </application>

</manifest>